(jbuild_version 1)

(library
 ((name        uecc)
  (public_name uecc)
  (synopsis "ECDH and ECDSA for 8-bit, 32-bit, and 64-bit processors.")
  (c_names (uECC uecc_stubs))
  (libraries (bigstring))))
